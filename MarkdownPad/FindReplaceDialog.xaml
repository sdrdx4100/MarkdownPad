<Window x:Class="MarkdownPad.FindReplaceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="検索と置換" 
        Height="200" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="検索:" VerticalAlignment="Center" Margin="0,0,8,8" />
        <TextBox x:Name="FindTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" />

        <TextBlock x:Name="ReplaceLabel" Grid.Row="1" Grid.Column="0" Text="置換:" VerticalAlignment="Center" Margin="0,0,8,8" />
        <TextBox x:Name="ReplaceTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" />

        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,8">
            <CheckBox x:Name="CaseSensitiveCheckBox" Content="大文字と小文字を区別" Margin="0,0,16,0" />
        </StackPanel>

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="FindNextButton" Content="次を検索" Width="80" Margin="0,0,8,0" Click="FindNextButton_Click" />
            <Button x:Name="ReplaceButton" Content="置換" Width="80" Margin="0,0,8,0" Click="ReplaceButton_Click" />
            <Button x:Name="ReplaceAllButton" Content="すべて置換" Width="80" Margin="0,0,8,0" Click="ReplaceAllButton_Click" />
            <Button Content="閉じる" Width="80" Click="CloseButton_Click" />
        </StackPanel>
    </Grid>
</Window>
